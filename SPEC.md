# CHIP1 로드셀 평가 유닛 펌웨어 — 구현 스펙 v4

**v4 변경 요약 (팀 지시): 내장 DAC 복원 + 캘리브레이션 기반 마련.**
v3 구성(Nucleo-H533RE ↔ CHIP1 센서보드 직결, 비트뱅잉 드라이버 + CLI)은 그대로
유지하고, STM32H533 내장 DAC1(PA4/PA5)로 자극 전압을 생성하는 기능과 지그 보정용
캘리브레이션 구조를 추가. **목적: 기존 DAC1220 셋업 테스트 결과와의 비교 검증.**

기존 펌웨어는 소스 유실로 처음부터 새로 작성. UART 명령 이름은 기존 체계
(wr/rr/rd, dac init/set) 유지 — PC측 파이썬 스크립트 재사용 가능성 대비.

## 0. 절대 규칙

- **CHIP1은 표준 SPI가 아님** (섹션 3). STM32 SPI 페리페럴 금지, GPIO 비트뱅잉.
- 핀 할당은 `app/board_pins.h` 한 파일에만 정의. 단 PA4/PA5는 DAC1 출력으로
  칩에 고정 (변경 불가).
- **PA5는 원래 LD2(사용자 LED) → v4부터 DAC1_OUT2 전용. LED 기능 전면 제거.**
  (CubeMX 재생성 시 main.c에 BSP_LED_Init가 되살아나면 다시 삭제할 것.)
- 개발/검증은 새 개발 보드에서. 기존 셋업 보드들은 백업 완료(board1/board2 .bin),
  레거시 셋업은 참고용으로 보존.

## 1. 하드웨어 구성 (2026-07-20 실측 검증 완료)

```
Nucleo-H533RE ── 점퍼선 6가닥 ──> 어댑터 보드(2.54mm) ──> CHIP1 센서보드
(로드셀 신호선은 AINP/AINN에서 분리 — 자극원을 내장 DAC으로 교체. 레거시 셋업과 완전 분리 확인)
```

### 최종 배선 (본선 6가닥 + 측정 2가닥, 실측 검증됨)

| Nucleo 쪽 | 상대측(어댑터) | 역할 |
|---|---|---|
| 3V3 (CN6) | DVDD | 전원 |
| GND (CN6) | DGND | 접지 |
| A0 (PA0) | SCK | 비트뱅잉 클럭 |
| A1 (PA1) | DOUT(SDA) | 비트뱅잉 데이터 |
| D1 (PA4, **개조**) | AINP | DAC ch1 |
| D13 (PA5) | AINN | DAC ch2 |
| A3 (PB0) | Nucleo 3V3 (보드 내 점퍼) | meas: DVDD 노드 (3.335V) |
| A4 (PC1) | **실크 AVDD 구멍 (C1 개조로 REFIN=VDDA 노드화)** | meas: VDDA/REFIN (3.0V) |

### PA4 인출 개조 (완료, 팀 승인)

- **SB19 제거 + SB22 실장** → PA4(DAC1_OUT1)가 아두이노 D1(CN9-2)로 인출됨.
- 사고/복구 기록: 납땜 중 **SB3**(T_VCP_TX 경로, SB22 옆칸)에 납이 번져 콘솔(VCP)
  불통 발생 → 통전 진단으로 특정, SB3 잉여 납 제거 후 콘솔 복구. SB2 무사 확인.
  **교훈: SB22 주변 재작업 시 SB3/SB2 통전 확인 필수.**
- 검증: SB22 통전 ✓ / SB19 개방 ✓ / SB3 개방 ✓ / D1↔GND 쇼트 없음 ✓ /
  `dac set 1`로 D1에서 지시 전압 실측 확인 ✓

### 센서보드 C1 브리지 개조 (2026-07-23 완료, 팀 지시)

- 사유: 실크 AVDD 구멍(J1-1)은 미실장 레퍼런스(U2/U3)용 **고립 라인**이라
  그대로는 부유 전압(1.2~1.4V, 밴드갭 누설)만 측정됨 — VREF 탭 불가.
- 개조: **C1(DNP)의 고립라인 쪽 패드 ↔ REFIN 노드 납 연결** → 실크 AVDD
  구멍이 REFIN=VDDA(3.0V) 노드에 편입.
  **⚠ C1 반대쪽 패드는 GND — 두 패드를 브리지하면 VDDA 단락. 절대 금지.**
- 검증: 구멍↔R1 우측 통전 ✓ / 구멍 3.0V 실측 ✓ / `meas` VREF=3.024V ✓ /
  `id` 0x9210 ✓
- 개조 보드에 마킹 있음. **미개조 센서보드에서는 이 구멍에서 VREF가
  1.2V대(부유)로 나오는 것이 정상이며, 그 측정값은 무효.**

- 콘솔: USART2 (PA2/PA3, ST-Link VCP), 115200 8N1.
- LD2(PA5)를 DAC 출력으로 쓰므로 **보드 LED 없음**. 생존 확인은 UART 프롬프트로.
  (LD2는 SB6+47K로 PA5에 연결 — 부하 ~70µA로 미미, 실측 오차 ±1mV 이내 확인됨)
- 전원 인가 → 1.5ms 후부터 CHIP1 인터페이스 사용 가능.
- 센서보드(EVM) 회로도: **(센서보드 회로도)** (U1=CHIP1, 파란 보드 실물과
  일치). ⚠ (레거시 회로도)는 레거시 캐리어 지그 캐리어 쪽 문서 — 혼동 주의.
  Nucleo 회로도: docs/mb1814-h533re-c02-schematic.pdf.

## 2. UART 명령 인터페이스 (CLI)

### 명령 (인자 10진수 기본 / 0x접두면 16진수)

| 명령 | 동작 | 응답 |
|---|---|---|
| `wr <addr> <val>` | CHIP1 레지스터 쓰기 (8bit) | `OK` |
| `rr <addr>` | CHIP1 레지스터 읽기 | `0x??` 줄 + `OK` 줄 |
| `rd <count>` | count개 샘플 캡처 | 샘플별 1줄 스트리밍, 마지막 `OK` |
| `id` | 칩 ID 읽기 (0x3E,0x3F) 편의 명령 | `0x9210` + `OK` |
| `iface [spi\|i2c]` | 칩 인터페이스 선택/표시 — spi=CHIP1(기본), i2c=CHIP1A (§3.5) | 현재 모드 또는 `OK` |
| `iscan` | I2C 주소 스캔 0x08~0x77 (iface i2c 필요) | 검출 주소 줄들 (없으면 `none`) + `OK` |
| `stop pd` / `stop wake` | STOP(파워다운) 전류 테스트 (§10): PD 진입·유지 / 복귀 | `OK` |
| `dac init` | DAC1 양 채널 활성화 + 0V 출력 | `OK` |
| `dac set <ch> <uV>` | ch=1,2. 보정 적용 후 µV 단위 출력 | `OK` 또는 `ERR out of range (max 3300000)` |
| `dac cal <ch> <offset_uV> <gain_ppm>` | 채널 보정계수 설정 (런타임) | `OK` |
| `dac cal show` | 현재 보정계수 + 채널별 플래시 상태 | 채널별 1줄 + `flash: ch1=saved ch2=none` + `OK` |
| `dac cal save [ch]` | 계수를 보드 플래시에 영구 저장 (ch 지정 시 그 채널만 갱신·반대쪽 보존) | `OK` 또는 `ERR flash save failed` |
| `uid` | MCU 96bit 고유 ID (보드 식별용) | `UID=XXXXXXXX-XXXXXXXX-XXXXXXXX` + `OK` |
| `meas` / `meas cal` | 노드 측정 / ADC 셀프캘 (§8) | 값 + `OK` |
| `help` | 명령 목록 | 목록 + `OK` |

### 출력 규칙

- NUL/ANSI 절대 금지, 순수 ASCII. 줄바꿈 `\r\n`. 수신은 `\r`/`\n`/`\r\n` 허용.
- modal 메뉴 없음, flat 프롬프트 `> `.
- 모든 명령은 `OK` 또는 `ERR <이유>`로 종료.
- `rd` 샘플: 한 줄 하나, 부호 있는 10진수 (24bit two's complement → int32 부호확장),
  줄마다 즉시 전송. 진행 중 ESC(0x1B) 수신 시 `ERR aborted`로 중단.
- 에러 예: `ERR unknown command`, `ERR bad arg`, `ERR adc no drdy (timeout)`,
  `ERR out of range (max 3300000)`

### 구현 메모

- UART RX 인터럽트 + 링버퍼, 라인 파싱.
- `rd 1024` @ 10 SPS ≈ 102초 = 정상. 샘플당 DRDY 타임아웃 500ms.

## 3. CHIP1 드라이버 — ⚠ 표준 SPI 아님 (HX711류 2선 펄스 프로토콜)

핀: SCK(출력), SDA(양방향 단선). GPIO 비트뱅잉. tSCK min 125ns → High/Low 각 1µs.
µs 딜레이 유틸(DWT 사이클카운터) 사용.

### 3.1 치명적 함정

- **SCK를 100ms 이상 High로 유지하면 Power-Down 진입.** SCK idle은 반드시 Low.
  GPIO 초기 상태 확인. 파워다운 해제는 SCK Low 후 350ms(tWU) 대기.
- 전원 인가(PoR) 후 1.5ms 대기.
- 데이터 업데이트 구간 tUD(≈32µs) 중 클럭 금지 → SDA 하강엣지 감지 후 읽으면 자동 회피.
- 채널 전환/입력 급변/첫 변환 후 2~3샘플 세틀링 무효. `rd`는 일단 버리지 않음
  (TODO 주석으로 폐기 옵션).

### 3.2 데이터 읽기 프레임 (1샘플)

1. SDA 입력 모드, **SDA Low 대기** = DRDY. 타임아웃 500ms → `ERR`.
2. SCK 펄스 1~24: 상승엣지마다 SDA 1비트 read, MSB first → 24bit two's complement.
3. SCK 펄스 25~27: 상태비트 ST0/ST1 읽고 SDA High로 강제됨. **항상 27펄스까지**
   (다음 DRDY 하강엣지 감지 위해). ST0=1 = 직전 사이클 설정 적용됨.
4. 부호확장: `if (raw & 0x800000) raw |= 0xFF000000;`

### 3.3 레지스터 접근 (읽기 프레임의 연장, 펄스 카운트)

27펄스 데이터 프레임에 이어서:
- 펄스 28~29: 설정 모드 진입
- 펄스 30~35: 6bit 레지스터 주소 (SDA 출력 전환, 호스트 드라이브)
- 펄스 36: SDA High=쓰기 / Low=읽기
- (펄스 37: 방향 전환 갭)
- 펄스 38~45: 8bit 데이터 (쓰기=호스트, 읽기=SDA 입력 전환 후 칩)
- 펄스 46: SDA High 강제, 종료

`wr`/`rr` = "샘플 1개 읽기 + 레지스터 접근" 한 프레임. `wr` 설정은 다음 변환
사이클부터 적용. `rr` 검증은 한 프레임 뒤에.

### 3.4 레지스터 맵 (필요분)

| ADDR | 이름 | 내용 |
|---|---|---|
| 0x00~0x02 | DOUTH/M/L | ADC 데이터 |
| 0x03 | DSMC | [7:4] CR: 0000=10SPS, 0001=40, 0010=500, 0011=1000, 0100=20, 0101=80, 0110=160, 0111=312.5 / [3:0] PGA: 0000=×1, 0001=×2, 0010=×64, 0011=×128(def), 0100=×4, 0101=×8, 0110=×16, 0111=×32, 1000=×256 |
| 0x04 | CSEL | [7:5]: 000=Differential(def), 001=Single, 011=Internal Short |
| 0x05 | CFGR | [6:5] MODE: 00=Normal / [0] DRDY (읽으면 클리어) |
| 0x2B / 0x32 | RCFG / WCFG | 간이 설정 읽기/쓰기 |
| 0x3E / 0x3F | IDH / IDL | 칩 ID = 0x92 / 0x10 (**통신 검증용**) |

참고 시퀀스 (레거시 워크플로우 검증 완료): `wr 0x03 0x02`=10SPS+PGA×64,
`wr 0x04 0x60`=Internal Short, `wr 0x04 0x00`=Differential.

### 3.5 CHIP1A (I2C 버전) 지원 (2026-07-29 신규, **동일 실기 id=0x9210 검증**)

- 데이터시트 기준: **CHIP1=SPI(2선 펄스) / CHIP1A=I2C** — 같은 8핀·같은
  레지스터 맵, 핀5가 SCK↔SCL. I2C는 슬레이브 주소 **0x2A**, fast-mode
  (≤400kHz), 바이트 주소 모드, CFGR의 AINC 비트로 주소 자동 증가.
  (주의: 데이터시트 rev 0.1 변경이력에 "I2C 문단 삭제"라 적혀 있으나 실제
  docx에는 섹션이 남아 있음 — 본 구현의 근거.)
- 펌웨어: `app/chip1a_i2c.c` — 같은 배선(PA0→SCL, PA1→SDA)에 비트뱅잉 I2C
  마스터 (~100kHz, open-drain+내부 풀업, 클럭 스트레칭 대응). 샘플 읽기 =
  CFGR bit0(DRDY, 읽으면 클리어) 폴링 → DOUTH/M/L 3회 읽기 (AINC 비트 위치
  미확인이라 버스트 읽기는 TODO — 10~80SPS에서는 tearing 위험 없음).
- `iface` 명령으로 런타임 전환, wr/rr/rd/id가 자동 라우팅. 부팅 기본 spi.
  **iface spi 복귀 시 350ms 웨이크 대기** (I2C idle=High 동안 SPI 칩이
  파워다운되므로). I2C 에러는 `ERR i2c nack`.
- PC측: autotest `--iface i2c` / GUI "Interface (칩 종류)" 셀렉션 (선택 즉시
  Run/스윕/캘/보드확인 전 경로 반영). 구펌웨어에 i2c 요청 시 명확한 에러.
- **엑셀 시트 분리 (2026-07-29, 08-07 대칭화)**: 결과가 칩 인터페이스별
  이름의 시트로 구별됨 — 상온: SPI=**`*_SPI`** 시트 (첫 SPI 테스트 시 기본
  시트를 `<이름>_SPI`로 개명, 데이터 보존) / I2C=**`*_I2C`** 시트 (첫 I2C
  테스트 시 SPI 시트 레이아웃 복제 생성). 스윕: 시트명에 `_I2C` 태그
  (`SW_MMDD_HHMM_I2C[_라벨]`). 블록 레지스터 표기에도 `, I2C` 병기.
  라우팅은 `_get_sheet()` 단일 지점 (CLI/GUI 공용).
  **중복 방지 규칙 (2026-08-07 실사고 수정)**: 기존 `*_I2C` 시트가 있으면
  활성 탭과 무관하게 재사용 (활성=260410_SPI일 때 260410_SPI_I2C를 또 만들던
  버그). 신규 생성 시 base의 `_SPI` 접미 제거. SPI 라우팅도 활성 탭이 I2C
  시트면 회피 (`_SPI` 시트 → 무접미 시트 순). 시트명 수동 변경(`..._SPI`)
  지원.
- **⚠ EVM R14 풀다운 함정 (2026-07-29 실사고)**: 센서보드 SCK 라인에
  R14(47k→GND, SPI idle-Low 안전장치)가 있어 내부 풀업(~40k)과 분압
  → SCL이 ~1.8V에서 멈춰 첫 시도는 전부 `ERR i2c nack`.
  **해결 = SCL 푸시풀 구동** (오픈드레인 대신 직접 High로 밀어 47k 무력화,
  외부 풀업 불요). 전제: 단일 마스터 + 클럭 스트레칭 없음. 만약의 충돌은
  EVM 직렬 100Ω(R13)이 제한. SDA는 오픈드레인+내부 풀업 유지, 클럭 50kHz.
- `iscan` 명령: I2C 주소 스캔(0x08~0x77) — 브링업/주소 확인용.
- 실기 검증 (2026-07-29): A칩 납땜 EVM에서 `iface i2c` → `iscan` 0x2A 검출
  → `id` **0x9210** 확인. (rd/wr 풀 경로 및 GUI Run은 후속 검증 중)

## 4. 내장 DAC1 드라이버 (v4 신규)

- STM32H533 DAC1: **ch1=PA4(DAC1_OUT1), ch2=PA5(DAC1_OUT2)**, 12bit, 버퍼드 출력.
- HAL DAC 모듈 미사용 — 레지스터 직접 제어 (MCR MODE=000 buffered+external,
  CR EN1/EN2, DHR12R1/R2). `app/dac_internal.c/h`.
- VDDA=3.3V 가정 (실측 TODO). 변환: `code = round(uV * 4095 / 3300000)`, 0~4095 클램프.
- 요청 전압 > 3,300,000µV → `ERR out of range (max 3300000)`.
- **표준 자극 (팀 확정, 유효성 검사용): `dac set 1 1520000` (AINP=1.52V) /
  `dac set 2 1480000` (AINN=1.48V) → 차동 +40mV.** 구 DAC1220 셋업 데이터와
  부호까지 일치 (Channel A 평균 +6.9M 근처 기대).

### 캘리브레이션 (지그 보정)

- 채널별 계수: `offset_uV` (기본 0), `gain_ppm` (기본 0 = ×1.0). 런타임 설정,
  재부팅 시 소실 — **영구화는 PC측 `dac_cal.json`으로 해결** (아래 2점 보정).
- 적용: `uV_corrected = (uV + offset_uV) * (1 + gain_ppm/1e6)` → 변환식 진입.
  보정 결과는 0~VDDA로 클램프.
- **⚠ 사용법: `dac cal`은 계수만 바꾸고 출력을 갱신하지 않는다. 반드시 `dac set`을
  재실행해야 새 계수가 반영됨.** (cal 시 마지막 set 값 자동 재출력 개선은 TODO)

### 2점 캘리브레이션 (2026-07-21 구현 — PC측 위저드)

- GUI(tools/chip1_gui.pyw)의 "DAC Calibration" 섹션: 채널 선택 → 보정 0 리셋 →
  저점(기본 0.5V) 출력·실측 입력 → 고점(기본 3.0V) 출력·실측 입력 → 계수 산출 →
  `dac cal` 전송 → 중간점(1.75V) 검증, **목표 ±2mV PASS/FAIL**.
- 수식: 실측 모델 `m = A·s + B` (2점에서 A, B) →
  `gain_ppm = round((1/A − 1)·1e6)`, `offset_uV = round(−B)`.
  합성 시뮬레이션(펌웨어 정수연산+12bit 양자화 포함) 검증: 최악 오차 0.33mV.
- 결과는 **`dac_cal.json`** (프로젝트 루트)에 채널별 저장 — CLI/GUI 모두 테스트
  시작 시 자동 로드해 `dac cal`로 주입 (없으면 §4 기본 계수 폴백).
- 저/고점 간격 최소 0.5V 강제 (넓을수록 기울기 정확), 실측 입력 검증 0~3.5V,
  기울기 0.5~2.0 밖이면 배선/채널 오류로 거부.

### 보드 내 영구 저장 + 보드 식별 (2026-07-24 신규)

**목적: 캘 계수가 보드를 따라다니게 — 어느 PC에 꽂아도, 터미널로 `dac set`만
쳐도 보정된 출력. "클릭만 아는 사람"도 쓸 수 있게.**

- **저장소**: 내부 플래시 마지막 8KB 섹터 (0x0807E000, bank2 sector31).
  링커에서 코드 영역을 504K로 줄여 예약 → 펌웨어와 충돌 불가. `app/cal_store.c`
  (magic+ver+계수2ch+CRC 32B). 코드와 다른 뱅크라 쓰기 중 실행 정지 없음,
  최악의 실패 = "저장 안 됨"(벽돌 위험 0). 쓰기 빈도=캘 이벤트뿐(수명 무관).
- **부팅 자동 로드**: `dac_internal_init`이 저장소에 유효 계수가 있으면 자동
  적용 (없으면 무보정). PC 주입 불필요.
- `dac cal save [ch]`: RAM 계수를 플래시 기록 (소거+프로그램+검증). **채널별
  valid 플래그 (v2 블롭, 2026-07-24)** — 한 채널만 캘해도 반대 채널 저장분을
  보존하며 머지, 미캘 채널은 "캘 OK"로 오표시되지 않음. v1 블롭은 양채널
  valid로 하위 호환. GUI 위저드가 PASS 후 해당 채널만 자동 save.
- GUI 사용성: 보드 감지가 **미캘 채널을 콕 집어 안내**(위저드 채널 자동 선택),
  한 채널 완료 시 **"반대 채널도 이어서?" 체이닝** 제안 — 클릭만 따라가면
  양 채널 완캘되는 흐름.
- **보드 식별**: `uid`(MCU 96bit UID). ⚠ UID/VREFINT 캘값은 엔지니어링 바이트
  (0x08FFF800~810) — ICACHE 켠 채 읽으면 HardFault → 1회 캐시 패턴 필수.
- **⚠ ICACHE 재발 (2026-07-24): 플래시 프로그램 직후 같은 영역을 읽으면
  ICACHE가 옛 내용(소거 전/0xFF)을 반환** → cal_store_save의 memcmp 검증이
  거짓 실패(HAL 에러는 0인데 verify 실패)했음. 프로그램 후 `HAL_ICACHE_Invalidate()`
  필수. (엔지니어링 바이트 함정과 별개 — 이건 일반 플래시 쓰기 후 읽기 문제.)
- **실증 (2026-07-24)**: 계수 저장 → 하드웨어 리셋 → 재입력 없이 `dac cal show`가
  저장값+`flash: saved` 반환. app_setup이 부팅 시 dac_internal_init 호출 →
  자동 로드. 이 보드 UID=32303639-30335114-0028002C, 실계수 flash 저장 완료.
- **PC 레지스트리** (config/board_registry.json): 이 PC가 본 보드들의 UID→캘
  이력. 진실은 보드 플래시고 이건 추적용. GUI가 연결 시 UID+`dac cal show`의
  flash 상태를 읽어 **"캘 OK ✓" 또는 "⚠ 미캘 → 캘 안내 팝업"**.
- **캘 적용 우선순위 (2026-07-24 리뷰에서 확정)**: 보드 플래시 saved →
  **json 주입 생략** (`dac_ready()` 공용 함수, CLI/GUI/스윕/ADC캘 4경로 통일).
  플래시 없을 때만 json/기본 계수 폴백. 이전에는 json이 무조건 주입돼 다른
  PC의 stale 계수가 보드 캘을 덮는 결함이 있었음 — 수정 완료.
- ⚠ 캘은 펌웨어 재플래시(섹터 소거)에는 살아남지만 **mass erase 시 소실** —
  full chip erase 후에는 재캘 필요.

### 엑셀 블록 메타 자동 기입 (2026-07-24)

- **블록 헤더 날짜 자동 갱신 (2026-08-07)**: 헤더의 날짜(`YYYY-MM-DD DUT#n`)는
  레거시 템플릿 박제값이었음 → 블록에 기록될 때 실제 테스트 날짜로 교체
  (@온도/[런태그] 접미 보존). 기존 블록의 과거 날짜는 소급 수정하지 않음.
- 레거시 템플릿의 수기값(Input V=5, AVDD=2.98 등)을 이번 런 실측으로 대체:
  r6=meas VDD, r7=meas AVDD(VREF), r8=PGA, r16=실지령 차동(`=1.52-1.48`),
  r17=VIN(mV). **수식 셀(r13 평균/r15 Vin계산/r18·19 AINN/AINP/r20 정확도)은
  절대 덮지 않음** — Run test/스윕 모두 자동.
- ⚠ r7 AVDD는 **실측값(3.024)** 기입이지만, 정확도 수식(r15)은 의도적으로
  **명목 3.0V 고정** (기존 데이터와 비교 일관성 — §8 "기존 데이터 유효" 근거).

### 확정 보정계수 (2026-07-20 실측, 이 보드 기준)

- 보정 전: 양 채널 공히 지시 대비 약 +10mV (VDDA가 3.3V보다 ~0.7~1% 높은 것으로
  추정되는 공통 gain 오차). 2회 반복 보정으로 수렴.
- **ch1: offset=0, gain_ppm=-10137 / ch2: offset=0, gain_ppm=-10529**
- 보정 후 실측: ch1 지시 1.480000 → 1.480V, ch2 지시 1.520000 → 1.519V (±1mV 이내)
- 부팅 후 재입력:
  ```
  dac init
  dac cal 1 0 -10137
  dac cal 2 0 -10529
  ```

## 5. 코드 구조 / 마일스톤

```
app/board_pins.h     ← 핀 매핑 (유일한 하드웨어 정의 지점)
app/delay.c/h        ← µs 딜레이
app/chip1.c/h       ← 비트뱅잉 드라이버
app/dac_internal.c/h ← 내장 DAC1 + 캘리브레이션 (v4)
app/meas.c/h         ← 내장 ADC 노드 측정 (온도 스윕용, §8)
app/cli.c/h          ← 라인 파서 + 명령 디스패치
tools/chip1_capture.py  ← rd→CSV 캡처 + 통계
tools/chip1_autotest.py ← 자동 테스트 (CLI) + 엑셀/cal/경로 공용 로직
tools/chip1_gui.pyw     ← GUI (테스트/DAC·ADC 2점 캘/온도 스윕, 로그 분할화면)
tools/chamber.py         ← 챔버 드라이버 (SU661/SH662 + Mock, §8)
tools/temp_sweep.py      ← 온도 스윕 상태머신 (§8)
tools/test_temp_sweep.py ← 상태머신 단위검증 (하드웨어 불필요)
tools/migrate_layout.py  ← 산출물 폴더 재정리 (일회성)
config/                  ← 보드 고유 캘 계수 (dac_cal.json, meas_cal.json)
results/                 ← 누적 엑셀 + sweeps/<런폴더>/ + manual/
```
(PC측 도구 사용법·폴더 구조 다이어그램은 FLASHING.md)

### meas 캘리브레이션 (2026-07-23 설계 변경: 내부 셀프캘 우선)

- **기본 = 셀프캘 (`meas cal` 명령, 배선 불필요)**: ADC 하드웨어 캘리브레이션
  (ADCAL) 재실행으로 오프셋, VREFINT 공장캘 재기준으로 게인/VDDA.
  **⚠ H5에는 VSSA 내부 채널이 없어서** 팀 지시의 "VSSA=0V 포인트"는 ADCAL
  엔진(ST 공식 오프셋 셀프캘, 온도 변화 시 재실행 권장)으로 대체 — 기능적 등가.
- GUI "Self-cal" 버튼: 실행 → meas 표시 → DMM으로 AVDD 구멍 실측 입력 →
  차이 ±5mV PASS/FAIL → config/meas_cal.json의 selfcal 항목에 이력 기록.
- 스윕 "온도별 자동 셀프캘" 체크박스(기본 ON): 각 온도의 TEST 단계에서
  `meas cal` 재실행 (온도별 ADC 드리프트 대응). CALFACTOR/VDDA 로그 기록.
- 외부 2점(임시배선, DAC ch1 기준 소스) 위저드는 **Advanced로 강등** —
  셀프캘 DMM 대조 실패 시의 백업. 계수는 vdd/vref 키로 저장돼 PC측 적용.
- 셀프캘 보정은 ADC 하드웨어 내부 적용이라 json은 기록용 (계수 주입 없음).
드라이버는 HAL GPIO/CMSIS만 의존, CHIP1 핀 주입, 하드코딩 금지.

마일스톤: ① LED+UART 에코 ✅ → ② CLI 프레임 ✅ → ③ `id`==0x9210 ✅ →
④ PA4 인출 개조 + 배선 ✅ → ⑤ DAC 멀티미터 검증 + 보정계수 산출 ✅ →
⑥ 엔드투엔드 검증 (DAC 자극 → ADC 코드 삼각검증) ✅ →
⑦ 기존 DAC1220 셋업 결과와 비교 ✅ (DUT#1~10, 2026-07-20~21 —
(비교 분석 자료)) →
⑧ 파이썬/엑셀 연동 자동화 ✅ (autotest/GUI/capture 3종).
**전 마일스톤 완료 — 유효성 보고서 문서화만 잔여.**

### 엔드투엔드 검증 결과 (2026-07-20)

- 조건: AINP=1.480V, AINN=1.520V (차동 −40mV), `wr 0x03 0x02` (10SPS, PGA ×64)
  — 주: 이 검증은 표준 자극(+40mV, 섹션 4)과 극성 반대로 수행됨. 본측정은 +40mV 사용.
- `rd 30`: 평균 약 −6.871M 코드, 산포 ±3K 수준, 드리프트 없음.
- **삼각 검증 성립: 지시 전압 = 실측 전압(±1mV) = ADC 코드(−40mV 상당).**
- ⚠ 관찰: 설정 직후 `rd`에서 −250K→−55K로 단조 드리프트하는 **과도 구간이 수십 초**
  존재 (PGA/설정 재적용 + 세틀링 후 정상화). 캡처 전 세틀링 대기 또는 초반 샘플
  폐기 필요 — rd 세틀링 옵션 결정에 반영할 것.

## 6. 미확정 / 확인 사항

| 항목 | 상태 |
|---|---|
| ~~PA4 접근 방법~~ | ✅ 해결 — SB19 제거+SB22 실장으로 D1 인출 (섹션 1) |
| ~~PA4/PA5 → AINP/AINN 배선~~ | ✅ 해결 — 섹션 1 최종 배선표 (실측 검증) |
| ~~VDDA 실제 값~~ | ✅ 실측 반영 — 3.3V보다 ~0.7~1% 높음, gain_ppm 보정으로 흡수 (섹션 4 계수) |
| ~~기존 DAC1220 셋업 결과와 비교 검증~~ | ✅ 완료 — DUT#1~10 (2026-07-20~21), 비교 분석 (비교 분석 자료). 보고서 docx 작성만 잔여 |
| ~~보정계수 영구화~~ | ✅ 해결 — dac_cal.json (PC측, 테스트 시작 시 자동 주입) |
| ~~2점 자동 보정~~ | ✅ 해결 — GUI 위저드 (섹션 4, 2026-07-21) |
| `dac cal` 시 마지막 set 값 자동 재출력 | TODO (현재는 cal 후 `dac set` 재실행 필요) |
| CHIP1A(I2C) 실기 검증 | ✅ id=0x9210 (2026-07-29, §3.5 — R14 풀다운은 SCL 푸시풀로 해결). rd/GUI 풀 경로 + AINC 비트 위치 확인 잔여 |
| rd 세틀링 샘플 폐기 옵션 (과도 구간 수십 초 실측됨 — 섹션 5 관찰) | 팀 확인 후 결정 |
| ~~2점 보정 위저드 실기 검증~~ | ✅ 완료 — 양 채널 실기 PASS, 검증오차 0.0mV (2026-07-21, dac_cal.json) |

## 8. 온도 챔버 연동 (2026-07-21 신규 — 팀 지시)

참고 문서 3종 (docs/): "Block diagram_Test system for CHIP1_251204A.pdf"
(프로토콜 실측 기록 p.3, CSV 포맷 p.5), "ESPEC_Chamber_Sx-xxx.pdf" (명령
매뉴얼 — 2026-07-22 확보), "Chamber Control Example Code.txt" (C# 예제).

### 챔버 프로토콜 (p.3 실측 기록 기준, tools/chamber.py)

| | SH-662 | SU-661 |
|---|---|---|
| 통신 | RS485, **9600** | Prologix GPIB-USB, **115200** |
| 종단 | **LF만 (CR 보내면 동작 안 함)** | LF/CR 모두 가능 (LF 사용) |
| 접두 | `1,` (예: `1,TEMP,S50`) | 없음 (`TEMP,S50`) |
| 초기화 | 불필요 | `++mode 1` `++auto 1` `++eos 3` `++addr 1` |
| 습도 | 지원 | **미지원** (`HUMI*` → `NA:CONTROLLER NOT READY-1`, MON? 습도 자리 빈 값) |

- 응답: set 계열 `OK:<echo>` / 거부 `NA:<사유>`. `TEMP?` → `현재,목표,상한,하한`.
  `MON?` → `온도[,습도],운전모드,알람수` — **매뉴얼상 SU 챔버는 습도 필드
  자체가 생략될 수 있음** (실측은 빈 필드). 드라이버는 3형태 모두 수용.
  운전모드: OFF / STANDBY / CONSTANT / RUN.
- `TEMP,S` 값: 소수 1자리 유효, **음수 표준 지원** (매뉴얼 예: `TEMP, L-20.0`).
  목표는 챔버 절대 상한/하한 사이여야 함.
- 명령 간 최소 간격 0.3s 강제 (매뉴얼 p.47 권고 0.1~1s), 응답 수신 후 다음
  명령 (매뉴얼 요구), 타임아웃 2s + 재시도 2회 (NA는 즉시 실패).
- 운전 시작 시퀀스 (C# 예제): `POWER,ON` + `HUMI,L0` + `HUMI,H100`
  (습도 미지원 SU661은 POWER,ON만) — `Chamber.start()`.

### 펌웨어 meas 명령 (유일한 펌웨어 변경)

- 내장 ADC1로 2노드 측정: **DUT DVDD = A3(PB0, INP9)**, **VDDA/REFIN =
  A4(PC1, INP11)** (board_pins.h 후보 — 배선 후 확정. A0/A1은 CHIP1 사용 중).
- **VREFINT 공장 캘리브레이션(0x08FFF810, 3.3V 기준) 기반 VDDA 실측 보정** —
  3.3V 가정 없이 매 측정마다 VDDA 산출. 16샘플 평균.
- 응답: `VDD=3.299V VREF=3.001V` + `OK` / 실패 시 `ERR meas failed`.
- HAL ADC 모듈 추가됨 (v1.7.0, ST GitHub — CubeMX 재생성 시 conf의
  HAL_ADC_MODULE_ENABLED와 CMakeLists의 adc 소스 유지 확인).
- **⚠ H5 함정 (2026-07-22 실사고): ICACHE 켜진 상태에서 VREFINT_CAL_ADDR
  (0x08FFF810, 엔지니어링 바이트)를 읽으면 HardFault** (BFAR로 확인).
  HAL의 `__LL_ADC_CALC_VREFANALOG_VOLTAGE` 매크로가 이 주소를 직접 읽으므로
  사용 금지 — meas.c는 부팅 후 1회만 ICACHE를 끄고 캘값을 캐시한 뒤 수동 계산.
  UID 등 같은 영역을 읽는 코드 추가 시 동일 처리 필요.

### VDDA/REFIN 노드 확정 (2026-07-22~23 실측, SCH_CHIP1_R1.2 기준)

- **U1 8번(VDDA)은 칩 내부 LDO 출력 = 3.0V 자체 생산** (C7 우측/R8 좌측/
  R1 우측 — 동일 노드 3.0V 통전·실측). **외부 공급 불필요, 3V3 주입 금지**
  (VDDA 상한 3.0V + LDO 출력 역주입).
- **REFIN=VDDA 쇼트는 R11(0Ω)로 공장 구현** (칩 1↔8번 핀 통전 실측 확인).
- meas 탭 확정: A3 = Nucleo 3V3 (DVDD 넷) / A4 = 실크 AVDD 구멍 —
  이 구멍은 원래 고립 라인이라 **C1 브리지 개조로 REFIN 노드화** (섹션 1
  개조 기록 참조). 1.2865V 미스터리 = 개조 전 고립 라인의 부유 전압.
- **정상 판정값: VDD=3.335V, VREF=3.024V** (기대 3.0V ±0.05).
- ⚠ 미개조 센서보드에서는 VREF 1.2V대(부유)가 정상 — **측정 무효** (개조
  보드에 마킹 있음).
- 온도 스윕은 **VREF 2.9~3.1V 범위 밖이면 경고 로그** (칩 미장착/미개조
  보드/접촉 불량 감지) — tools/chip1_gui.pyw 스윕 TEST 단계에 구현.
- **기존 캡처 데이터 전부 유효 (확정)**: 엑셀 Accuracy 수식이 VREF=3.0V 전제로
  100% 정합 → 측정 기간 내내 VDDA/REFIN=3.0V(내부 LDO)였음이 소급 확인됨.
  재캡처 불필요.

### 온도 스윕 (GUI "Temperature Sweep" + tools/temp_sweep.py)

- 입력: 온도 리스트(기본 -40,-20,0,10,25,40,70,85), 포화(분), 허용오차(±1.0°C),
  프로파일(SH662_RS485/SU661_GPIB/MOCK), 챔버 포트.
- 상태머신: SET_TEMP → WAIT_TARGET(10s 폴링, 90분 타임아웃) → SOAK →
  TEST(기존 Run test 로직 + `meas` 기록) → REPORT → 다음 온도.
- 중단 2모드: 즉시 / 현재 스텝(기록까지) 완료 후. 진행 상태(상태·챔버온도·
  남은 포화·진도 n/m)는 GUI 라벨 + 로그 실시간 표시.
- 종료 동작 (2026-07-23 추가, 방치 방지): `25C 복귀 후 정지`(기본) /
  `즉시 정지` / `유지(수동)`. 정상 완료 시에만 복귀 대기(±2°C, 최대 60분),
  중단·에러 시엔 즉시 POWER,OFF. 동작 실패 시 "수동 확인 필요" 경고 로그.
- 리포트 (**스윕마다 전용 엑셀 시트**, 2026-07-23 변경): 스윕 시작 시
  `SW_MMDD_HHMM[_라벨]` 시트를 새로 생성해 (레이아웃 복제+데이터 클리어)
  그 안에 온도별 블록 기입 + 헤더에 `@-40C` 라벨. **상온 검증 시트(260410)는
  불변.** EVM/칩 구분은 GUI 라벨 입력란 → 시트명+CSV명에 반영.
  `sweep_report_*[_라벨].csv` (p.5 포맷 2안: 챔버 세팅/측정온도, VDD/VREF
  +vref_valid, 마지막 wr 명령 ≤3개, rd mean/stdev/count, 블록/원본CSV 참조).
- 단위검증: tools/test_temp_sweep.py — MockChamber+가상시계로 정상 흐름·
  즉시/스텝후 중단·도달 타임아웃·허용오차 판정 5종 PASS.

### 실챔버/실기 검증 필요 (미검증 체크리스트)

| # | 항목 |
|---|---|
| ~~1~~ | ~~SH662 통신~~ ✅ 실측 완료 (2026-07-23): 쿼리(`TEMP?`/`MON?`) 파서 일치, `POWER,ON`→STANDBY→CONSTANT 전환, `POWER,OFF`, `NA:PARA ERR` 거부 형식, **음수 포함 `TEMP,S` 정수형** 전부 OK. 스윕 온도는 챔버 한계(−70~165) 내 |
| 2 | SU661: Prologix 초기화 시퀀스 후 TEMP,S 동작, ++auto 1로 응답 자동 수신되는지 |
| ~~3~~ | ~~음수/소수 온도 형식~~ ✅ **실측으로 정정 (2026-07-23): 이 SH-662 개체는 정수형만 수용** — `S25.0`/`S-40.0`은 `NA:PARA ERR`, `S25`/`S-40`은 OK (매뉴얼의 "소수 1자리" 표기와 다름, 실측 우선). 드라이버는 `:g` 포맷이라 정수 온도는 자동으로 정수형 전송 — **소수 온도(예: 25.5) 사용 금지** |
| ~~4~~ | ~~운전상태 문자열~~ ✅ 매뉴얼로 해결 — OFF/STANDBY/CONSTANT/RUN. 폴링 10s는 권고 간격(0.1~1s) 대비 충분히 여유 |
| ~~5~~ | ~~meas 배선/대조~~ ✅ 완료 — A3=3V3(3.335V, DAC캘과 교차일치), A4=VDDA(C7 패드 탭, 3.0V 실측). 위 "VDDA/REFIN 노드 확정" 참조 |
| 6 | 저온(-40)에서 보드/센서보드 동작 및 결로 대책 (케이블 인출 방법) |
| 7 | 장시간 스윕 중 USB/시리얼 안정성 (온도 스텝마다 보드 포트 재오픈으로 완화) |
| 8 | SU661 MON? 실응답이 빈 필드형인지 생략형인지 (드라이버는 둘 다 수용하나 기록용) |

## 10. STOP(파워다운) 전류 측정 (2026-08-10 신규 — 패키지 시료 비교)

**목적**: new bonding diagram 8SOP 신규 시료 vs 기존 시료의 STOP 전류
비교 (각 3개, 팀 테스트 플랜). 시퀀스: power on → SCK 0 → 2ms(resetb
1.5ms+eFuse 0.5ms) → SCK 1 → 500µs(>200µs PD 진입) → VDD 전류 측정.

- **하드웨어 (EVM+클립 아님)**: 칩을 **캐리어 지그의 소켓**에
  장착. Nucleo에서 SCK/GND 점퍼 (SDA는 id 확인용 선택 — 없으면 GUI가
  경고만 하고 진행). **DMM = Rigol DM3058E, USB(USBTMC)로 PC 연결**,
  DVDD 공급선과 **직렬**(µA 단자) — 캐리어 지그의 DVDD 점퍼 헤더(J207) 자리에
  삽입.
- 펌웨어: `stop pd` = 시퀀스 실행 후 SCK High **유지** (PD 지속, 이때 DMM
  읽기) / `stop wake` = SCK Low + 350ms 복귀 (푸시풀 복원). SPI 모드 전용.
- **SCK 구동 = 오픈드레인 (진짜 최종 2026-08-11 저녁)**: `stop pd`의 High
  = 핀 해제 → **캐리어 지그 캐리어 내장 4.7k 풀업**이 라인을 VDD(5V)로 올림.
  ① High=5V로 VIH 스펙 정식 충족 ② 라인=레일 전위라 내장 풀업 전류 0 →
  **빈 소켓 오프셋 ≈ 0** (팀 요구 충족). 외부 풀업/레벨시프터 불필요.
  ⚠ 푸시풀 3.3V로 밀면 내장 풀업을 통해 (5−3.3)/4.7k=362µA가 상시 누설되어
  베이스라인을 오염시킴 — 이 발견이 OD 확정의 결정적 근거.
  (빈 소켓 서명: pd ≈ 0mA / wake ≈ 1.06mA(5V/4.7k, Low로 눌린 A0로 새는
  것 — 측정은 pd에서만 하므로 무해).)
- PC: `tools/dmm_dm3058.py` (pyvisa, 자동탐지 0x1AB1, `MOCK` 리허설 모드.
  의존성: `pip install pyvisa` — VISA 미설치 PC는 `pyvisa-py pyusb
  libusb-package` 추가). GUI "Stop Current" 섹션: 시료 라벨/횟수 입력 →
  측정 → **results/stop_current.csv** 누적 (datetime, label, avg/min/max µA).
- **핵심 발견 (2026-08-13, 수동 검증 확정): 기존 패키지 칩은 5V에서 STOP
  지령(High)에도 0.55~0.67mA에서 정체** — 정상 STOP(µA)으로 못 내려가는
  전압 의존 누설 (High 감지는 부분 동작: 부유 0.93mA → 직결 High 0.56mA).
  3.3V에서는 기존도 0.17µA 정상 → 결함은 5V에서만 발현. 신규는 3.3V/5V
  모두 정상 (0.33µA@5V) = **약 1/1700 개선, 종전 597µA 데이터와 정합**.
  ⚠ 단위 함정 실사고: DMM mA 표시를 µA 시트에 그대로 적으면 1000× 오류
  (STOP 표=µA로 환산 기입, Normal 표=mA). 보고표 = 요약 시트(동작성
  매트릭스) + 수동 시트 2종 (3V3/5V × STOP·Normal 표).
- **레벨시프터 대비 (2026-08-13)**: 펌웨어 `stop pd pp`(SCK 푸시풀 High —
  TXB류 포함 전 시프터 호환) + GUI "VDD 조건"(3.3V 직결/5V 레벨시프터)
  + CSV `vdd` 컬럼(구행=3.3 취급, 라벨+전압 단위 갱신) + 보고표 `5V_자동`
  시트 자동 생성. 시프터 장착만 하면 5V도 GUI 원버튼. 판정 기준 = Test
  Plan 'STOP' **VDD < 10µA @5.0V** (신규 0.32µA PASS / 기존 553µA FAIL).
  상세 이력: (내부 기록).
- **팀 확정 (2026-08-12): 자동화 = VDD 3.3V + SCK 3.3V(내부 풀업)** —
  신호·전원 레벨 일치로 입력단 관통 전류 원천 차단, 외부 부품 0개.
  5V 조건 측정은 수동(보고표 5V_수동 시트에 기입), 레벨시프터 확보 후
  자동화 예정. 공급 탭은 TEST BASE J2의 3V3 쪽으로 이동.
- 측정 구성 (2026-08-11 저녁, 5V 시절): TEST BASE·캐리어 **분리**, 5V는
  TEST BASE J2(5V_VDD 핀) → DMM 직렬 → J207 칩쪽 핀 직결 (모듈·40핀·외부
  풀업 전부 퇴장). SCK는 위 오픈드레인 방식 — 빈 소켓 오프셋 ≈ 0이라
  측정값 = 칩 단독 STOP 전류.
- ⚠ 전원 인가("power on") 단계는 수동 — 캐리어 전원 구성에 따름 (Nucleo
  GPIO 전원 스위칭 없음).

## 9. 참고: 레거시 셋업 구조 (이해용, 새 펌웨어와 무관)

기존: 보드1(Nucleo) → TEST BASE의 FXMA2102 레벨시프터 → SLG46824G GreenPAK ×4
(표준 SPI를 단선 SDIO로 변환, SS1~SS4로 CHIP1/DAC1220×3 라우팅) → 40핀 커넥터
→ 캐리어(DUT 소켓 + DAC1220 모듈 + 납땜 CHIP1). 자극 전압은 DAC1220(20bit)이 생성.
기존 펌웨어는 표준 SPI 페리페럴 사용, 타깃 USB CDC로 PC 통신.
회로도: (레거시 회로도), docs/(레거시 회로도).
백업: board1_dac_backup.bin, board2_test_backup.bin.
